// This file is automatically generated, please do not alter it.
// 2025-12-07 13:42:18
#pragma once

inline auto GAUSSIAN_BLUR_PASS_FRAG_STR =
	"#version 310 es\n"
	"precision mediump float;\n"
	"precision highp int;\n"
	"layout(binding = 1, std140) uniform LocalInfo\n"
	"{\n"
	"    highp float radius;\n"
	"    int direction;\n"
	"} localInfo;\n"
	"layout(binding = 0) uniform highp sampler2D targetTex;\n"
	"layout(location = 0) in highp vec2 vUV;\n"
	"layout(location = 0) out highp vec4 fragColor;\n"
	"void main()\n"
	"{\n"
	"    highp float sigma = localInfo.radius;\n"
	"    int support = int(sigma * 3.0);\n"
	"    highp vec2 texSize = vec2(textureSize(targetTex, 0));\n"
	"    highp vec2 loc = vUV;\n"
	"    highp vec2 dir;\n"
	"    if (localInfo.direction == 0)\n"
	"    {\n"
	"        dir = vec2(1.0 / texSize.x, 0.0);\n"
	"    }\n"
	"    else\n"
	"    {\n"
	"        dir = vec2(0.0, 1.0 / texSize.y);\n"
	"    }\n"
	"    highp vec4 acc = vec4(0.0);\n"
	"    highp float norm = 0.0;\n"
	"    int _69 = -support;\n"
	"    for (int i = _69; i <= support; i++)\n"
	"    {\n"
	"        highp float coeff = exp((((-0.5) * float(i)) * float(i)) / (sigma * sigma));\n"
	"        acc += (texture(targetTex, loc + (dir * float(i))) * coeff);\n"
	"        norm += coeff;\n"
	"    }\n"
	"    acc *= (1.0 / norm);\n"
	"    fragColor = acc;\n"
	"}\n"
;